// Smart batch preview hooks with intelligent caching
// These are the only preview hooks needed - they handle individual combinations efficiently

export { useSmartBatchPreviewAssignment, useSmartBatchPreviewMapExploration } from './useSmartBatchPreview';